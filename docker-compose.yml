services:
  # Base de données PostgreSQL
  db:
    image: postgres:16-alpine
    container_name: mailkern_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: mailkern
      POSTGRES_PASSWORD: mailkern
      POSTGRES_DB: mailkern
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mailkern"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis pour cache et files d'attente
  redis:
    image: redis:7-alpine
    container_name: mailkern_redis
    restart: unless-stopped
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend FastAPI
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: mailkern_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      # Configuration PostgreSQL
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      POSTGRES_DB: mailkern
      POSTGRES_USER: mailkern
      POSTGRES_PASSWORD: mailkern
      # Configuration Redis
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./backend:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  # Frontend Next.js
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: mailkern_frontend
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next

volumes:
  postgres_data:
    driver: local
